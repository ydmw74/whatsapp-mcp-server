# Optional: Custom directory for WhatsApp auth session data
# Default: ~/.whatsapp-mcp/auth
# WHATSAPP_AUTH_DIR=/path/to/auth/dir

# Optional: Linked-device label shown in WhatsApp (Linked devices), e.g. "MyAgent-1"
# Note: WhatsApp will still show the OS in parentheses, e.g. "MyAgent-1 (Mac OS)".
# WHATSAPP_DEVICE_NAME=MyAgent-1

# Optional: Force re-linking in non-interactive environments (e.g. when started by an MCP client)
# - "backup" (default behavior for truthy values): moves existing auth dir to a timestamped backup and creates a new link
# - "delete": deletes existing auth dir and creates a new link
# WHATSAPP_RELINK=backup

# Optional: When you run the server manually in a terminal to scan a QR code, exit automatically after pairing
# Defaults to "auto": enabled only when stdin+stdout are TTY (manual runs), disabled for stdio-spawned MCP clients.
# WHATSAPP_EXIT_AFTER_PAIR=1

# Optional: Persist observed messages for whatsapp_list_messages to disk (message-store.json)
# WHATSAPP_PERSIST_MESSAGES=1
#
# Optional: Retention limits for message store
# WHATSAPP_MAX_MESSAGES_PER_CHAT=200
# WHATSAPP_MAX_MESSAGES_TOTAL=2000
